package Strategy;


import javax.swing.*;

/***
 * AlignStrategy is the StrategyInterface character in "Strategy Pattern".
 ---------------------------------------------------------
 Define executeAlign() method.
 ***/

public interface AlignStrategy {
    public void executeAlign(JTextArea textArea);
}
